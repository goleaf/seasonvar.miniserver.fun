# Реестр технического долга

Аудит: 18.07.2026; повторно сверен 20.07.2026 и 24.07.2026. Реестр не заменяет обязательную текущую работу: безопасно исправимые нарушения исправляются в соответствующем change set. Категория сложности — относительная (`small`, `medium`, `large`), без обещания срока.

| ID | Описание / категории | Модули | Риск и влияние | Предлагаемое решение | Зависимости / сложность | Статус и причина отсрочки | Критерий завершения |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TD-001 | Node 26 указан как build runtime, но находится в Current, а machine-readable pin отсутствует. Категории: production/frontend. | npm, Vite, Tailwind, deployment | Compatibility: build host может получить несовместимый npm/Node без явного изменения репозитория. Security/performance impact сейчас не подтверждён. | Отдельно проверить Node 24 LTS, добавить согласованный pin/range и повторить clean locked install, build, manifest и browser smoke. | Build image/control panel access; medium | Open. Runtime migration нельзя смешивать с package patches без rollback evidence. | Утверждённая LTS-линия зафиксирована в runtime matrix и machine-readable config, production/build проверены. |
| TD-002 | Для Composer self-update не установлены public keys. Категории: security/production. | Composer operator runtime | Security: `composer diagnose` не может подтвердить подписи release metadata для self-update. Project install/audit не объявляются уязвимыми. | Установить официальные keys на каждом build host по Composer guidance; не хранить credentials/keys как секрет приложения. | Operator access; small | Open / environment-owned. Репозиторий не должен подделывать machine trust state. | `composer diagnose` сообщает OK для tags/dev pubkeys на утверждённых hosts. |
| TD-003 | В global npm config существует deprecated `--init.module`. Категории: production/frontend. | npm operator runtime | Maintenance: следующий npm major перестанет поддерживать ключ; приложение не использует его. | Установить происхождение и удалить только внешний key, затем повторить audit/outdated/build. | Operator config; small | Open / external environment. Repository `.npmrc` не содержит ключ. | Три npm-команды проходят без предупреждения. |
| TD-004 | Полный legacy Larastan profile содержит накопленные diagnostics вне bounded required profile. Категории: architecture/PHP. | Широкий backend scope | Compatibility/maintenance: сложнее готовить большие PHP/framework changes. Нулевой required profile остаётся отдельным gate; текущая безопасность не выводится из количества diagnostics. | Разбирать по bounded module, не подавлять broad baseline и не смешивать с package major upgrades. | Module ownership; large | Open. Полная механическая перепись противоречит safe-scope policy. | Full profile достигает согласованного zero/baseline-free состояния без behavior drift. |
| TD-005 | Maximum Rector profile предлагает изменения в 1,337 files при analyzer errors `0`; это advisory modernization inventory, а не автоматическая migration. Категории: architecture/PHP. | Backend and protected test sources | Compatibility: массовые strict-types/readonly/naming/helper/facade proposals могут менять public/framework behavior без feature evidence. | Разбирать bounded modules; переносить только подтверждённые official deprecations в deprecation registry с отдельными decisions. | Official API guidance; large | Open. Исторический dry-run exit `2` означает proposed diffs; повторный запуск 19.07.2026 завершил workers без ошибок, но coordinators не завершились и были остановлены после более шести минут без output. Исторические 1,337 files не объявляются свежим результатом; Task 29 ничего не применяет и не запускает tests. | Подтверждённые deprecations вынесены в records; остальные proposals approved по модулям либо retained with reason; bounded maximum dry-run завершается воспроизводимо. |
| TD-006 | Production SQLite/Redis/provider state подтверждён только частично; off-host restore, HA/failover и реальные provider credentials/delivery не проверены. Категории: database/cache/production. | Database, cache, sessions, queues, mail/storage/provider boundaries | Production/data risk: доступный host evidence подтверждает версии и соединения, но не доказывает восстановление вне текущего volume, отказоустойчивость, provider delivery или полную rollback path. | Снять redacted dated preflight, выполнить isolated off-host restore rehearsal и отдельно проверить каждый реально настроенный provider перед соответствующим runtime/package group. | Authorized production/provider access; medium | Open / host evidence обновлён 24.07.2026, restore/provider evidence unavailable. | Runtime matrix содержит dated host evidence, off-host restore/rollback и проверенное connection/delivery behavior для каждого заявленного provider. |
| TD-007 | Safari, Firefox, iOS Safari и Android real-device journeys не выполнены локально. Категории: mobile/player/accessibility/PWA. | Player, Livewire navigation, uploads, responsive UI | UX/compatibility: Chromium smoke и build не доказывают media/PiP/fullscreen/safe-area behavior. | Выполнить documented device matrix при следующем affected frontend/player release. | Real devices/browser farm; medium | Open / unavailable verification. | Dated manual evidence для поддерживаемых browser families и affected flows. |
| TD-008 | Production cache/session/queue rollback evidence зависит от фактических driver/server settings. Категории: cache/production/privacy. | Cache, sessions, Redis queue, pending jobs | Data/privacy/availability: serializer or class changes may strand keys/jobs. Task 29 форматы не меняет. | Перед соответствующим update снять prefixes/serializer/pending-job/restart evidence и version keys where needed. | Production access; medium | Open / no current serialization change. | Каждый affected update decision содержит stale-key, session and pending-job transition. |
| TD-009 | Legacy Russian-only operator/admin/internal copy остаётся в application classes, включая catalog administration и CLI diagnostics. Категории: translations/administration/operations. | Admin catalog, stats, importer/operations commands and notifications | Multilingual maintenance: массовая замена без editorial/context review может испортить Russian operator contract; новые upgrades не должны увеличивать набор literals. Security/performance impact отсутствует. | Переносить coherent surface за surface в existing PHP catalogs с RU/EN key/placeholder parity; внутренние exception/log codes отделять от UI. | Editorial review; large | Open. Root project contract сохраняет Russian visible operations, а Task 29 исправил bounded Form Request/card cluster, но не переписывает 148 heterogeneous files. | User/admin surfaces получают catalogs и parity; intentionally Russian-only CLI/log messages документированы как operator contract. |
| TD-010 | Исторически Memcached hot-cache tier был недоступен; повторный аудит 24.07.2026 подтвердил Memcached `1.6.39` и рабочее соединение приложения, но отсутствующие cache-warm workers всё ещё не доказывают устойчивый warming/capacity contract. Категории: cache/production/operations. | Hot cache, cache warming, health summary | Availability/performance: transport снова доступен, однако backlog/heartbeat state не подтверждает нормальный прогрев и capacity. Потери persisted domain data нет, потому что tier остаётся disposable. | После восстановления process ownership проверить pool, serializer, prefix, TTL, fallback metrics, warming inputs и sustained hit/load behavior; не превращать Memcached в persisted source of truth. | Operational access and Task 5; medium | Open / transport availability verified 24.07.2026, warming/capacity evidence pending. | Health/warming подтверждены без degraded failures при штатных workers и измеренном capacity либо hot tier явно сохранён как optional fallback с утверждённым бюджетом. |
| TD-011 | Финальный read-only Chromium smoke под активной SQLite/queue-нагрузкой получил `200`, но главная заняла `39.5 s`, а первый mobile `/titles` — `52.0 s`; после reload `/titles` вернул `200` за `9.15 s`, а `/` превысил 20-секундный timeout. Стабильные follow-up локализовали correlated card-count hydration на homepage и в каталоге, включая count-based sorting. Категории: performance/catalog/production. | Home, catalogue, public page cache, SQLite contention, cache warming | Availability/UX: correctness подтверждена, но historical tail latency может выглядеть как outage. Security/data impact не обнаружен. Homepage/default card hydration и outer correlated aggregates `episodes_desc`, `seasons_desc`, `with_video` устранены; остаточная стоимость media aggregate и общий contention остаются открытыми. | Сохранить batch hydration и visibility-aware grouped count-sort joins; повторить desktop/mobile cold/warm matrix без timeout masking или общего cache clear. Дальнейшую оптимизацию `with_video` выполнять только по новому query-plan evidence, не денормализовать счётчики предположительно. | Stable production window and redacted timing evidence; medium/large | Open / частично устранён 20.07.2026. Homepage direct builder дал `1 355,92 ms` и `0` correlated counts. Catalog `per_page=96` card hydration изменился с `5 478,85 ms`/одного `5 177,83 ms` query до `243,63 ms`. Same-snapshot count-sort result comparison сохранил 96 ID: episodes `1 166,33→697,86 ms`, seasons `200,54→175,30 ms`, video `4 348,99→2 826,29 ms`; paginator больше не materializes aggregate повторно, но полный `with_video` builder всё ещё занял `3 586,10 ms`. Значения diagnostic, не p95/SLA. | Повторяемый desktop/mobile matrix на согласованном load profile укладывается в утверждённый бюджет, остаточный media-sort cost имеет решение или утверждённый бюджет, а cold/warm rollback и cache/SQLite contention документированы. |
| TD-013 | Production title group `#43428` содержала 30 sibling pages и около 1,93 MiB staged payload: page apply после checkpoint занял около 11 секунд, но прежний merge 29 duplicate seasons, 930 episodes и 957 media одной транзакцией превысил `timeout=900`, удерживал SQLite writer и вызвал lock/retry churn соседних финализаторов. Категории: importer/database/queue/performance/production. | Seasonvar title-group apply, catalog merge, calendar observations, SQLite writers, Redis retry | Availability/performance: durable per-page и per-season checkpoints уже устраняют полный повтор; последний season остаётся атомарным с удалением duplicate title, поэтому targeted retry сохраняет family identity. Подтверждённых потерь данных, security/privacy impact или failed jobs нет. Остаточная write amplification и tail latency одного большого season/final title transaction под параллельной SQLite-нагрузкой пока не имеют утверждённого бюджета. | Снять stage-specific query/transaction profile на крупнейшей поддерживаемой группе; сократить повторные reads/writes, сгруппировать bounded calendar/upsert work и оценить SQLite-specific serialization/chunking. Не повышать timeout и не уменьшать worker pool без измеренного compatibility/throughput решения. | Стабильное production-like окно, representative payload и merge/calendar correctness evidence; large | Open / checkpoint correctness реализована 20.07.2026, дальнейшая оптимизация требует отдельного design и performance change set. Она не скрывается повышением timeout или очисткой очереди. | Крупнейшая утверждённая группа укладывается в stage/attempt budgets либо доказанно продвигается bounded checkpoints без lock storm; query/write budget, crash-resume, merge и calendar correctness воспроизводимо подтверждены. |
| TD-014 | Redis `BGSAVE` 24.07.2026 оставался активным `105253` секунды, child потреблял примерно одно ядро, AOF отключён, а после последнего подтверждённого сохранения накопилось `10641008` изменений. Категории: cache/queue/production/data-safety. | Redis cache, sessions, queues, locks, persistence, process health | Availability/data risk: авария до успешного snapshot может потерять transport/session/queue state; зависший child отнимает CPU и делает обычный restart небезопасным без evidence. | Сначала добавить safe persistence diagnostics, снять host/storage/process evidence, проверить off-host recovery artifact и только в approved window выполнять controlled termination/restart по точному PID с post-recovery validation. | Tasks 1–3 master plan; large | Open. Read-only audit 24.07.2026 не изменял process или Redis state. | `rdb_bgsave_in_progress=0`, новый `rdb_last_save_time`/успешный snapshot подтверждены, queues/sessions/locks проверены, restart/restore path задокументирован и воспроизведён. |
| TD-015 | Intended `seasonvar-import`, `seasonvar-title-refresh` и `cache-warm-v2` workers не имеют heartbeat, при этом Redis содержит `43105` pending и `29045` delayed jobs; одновременно настроены `schedule:work` и cron `schedule:run`. Категории: queue/importer/cache/production. | Scheduler, systemd/process manager, importer, title refresh, cache warming | Availability/freshness risk: producers продолжают создавать работу без штатных consumers, двойной scheduler может дублировать dispatch, а резкий запуск полного worker pool способен вызвать SQLite lock storm. | Выбрать одного scheduler/process owner, остановить duplicate producer path и поднимать workers ступенчато `1→2→4…` только при отрицательном backlog trend и приемлемых lock/latency/error metrics. | Redis recovery and operator access; large | Open. Аудит не запускал и не останавливал процессы. | Ровно один scheduler/process owner, текущие heartbeats всех intended pools, backlog/oldest age в утверждённом бюджете без lock storm и duplicate dispatch. |
| TD-016 | В `failed_jobs` находится 33 597 записей; 12 851 parsed finalizers классифицированы как terminal `forget_candidate`, а 1 693 350 import events и 33 108 terminal groups старше семи дней показывают retention debt. Категории: importer/queue/database/storage. | Failed jobs, import finalizers, event retention, SQLite growth | Data/operations risk: массовый retry может повторить необратимые side effects, массовое удаление уничтожит forensic evidence, а зависимый от active-import pruning не удерживает технические таблицы в бюджете. | Добавить exact-ID dry-run disposition с reason codes, запретить `retry all`/`flush`, отделить scheduled retention от активного импорта и удалять только подтверждённые terminal rows bounded chunks после backup. | Tasks 6–9 master plan; medium/large | Open. Ни одна failed/event row planning-задачей не изменена. | Failed jobs имеют reviewed exact disposition, terminal retry candidates отсутствуют или обоснованы, retention запускается независимо и старые technical rows остаются в утверждённых окнах. |
| TD-017 | Около 48 GiB backups хранятся на том же volume, что и приложение/SQLite; off-host copy и isolated restore rehearsal не подтверждены. Категории: backup/storage/production/data-safety. | SQLite, private storage, backups, restore, deployment rollback | Disaster-recovery risk: локальные копии не защищают от потери volume и не доказывают, что база/приватные файлы/manifest совместимы и восстанавливаются в заявленное окно. | Создать manifest/checksum, скопировать approved artifact во внешний destination без secrets, выполнить isolated restore и integrity/application smoke, измерить RPO/RTO и только затем разрешать cleanup/compaction. | External backup destination and operator window; large | Open / external destination and restore evidence unavailable. | Проверенный off-host artifact, успешный isolated restore, задокументированные RPO/RTO и tested partial/full rollback. |
| TD-018 | SQLite database занимает около 28,15 GiB; 100 demo users владеют миллионами comments/reactions/reviews/states, synthetic timestamps доходят до 2037 года, а принадлежность corpus к реальным данным ещё не доказана. Категории: database/demo/privacy/performance. | Demo data, user content, SQLite, indexes, backups, retention | Data/privacy risk: механическое удаление по email/date может затронуть реальные rows или нарушить counts/foreign keys; объём увеличивает backup/integrity/cold-query cost. | Ввести явные `curated|load_test` профили, построить read-only reconciliation по stable ownership и cross-table counts, потребовать отдельное data authorization, затем bounded cleanup и copy-and-swap compaction с verified rollback. | Verified restore and explicit cleanup approval; large | Open. Planning-задача не удаляла и не переклассифицировала rows. | Demo/load-test identities доказуемо отделены, reconciliation сходится, approved cleanup не затронул real users, post-swap integrity/counts/size/latency проверены. |
| TD-019 | Writable/cache paths содержат сотни `root:root` compiled views, режимы `777`; logs фиксируют `touch(): Utime failed`, а ранее отсутствовал Vite manifest до локальной rebuild. Категории: permissions/deployment/security/frontend. | `storage`, `bootstrap/cache`, release assets, PHP-FPM/deploy user | Availability/security risk: mixed ownership ломает cache/view writes, `777` расширяет запись, а неатомарные assets могут отдавать HTML со ссылками на отсутствующий manifest/chunk. | Добавить allowlisted permission dry-run/fix command, запретить runtime `777`/root cache artifacts, собирать code+manifest+hashed assets как один release artifact и активировать/откатывать атомарно. | Tasks 13–14 and deployment ownership; medium/large | Open. Audit only; permissions/assets не менялись. | Runtime trees имеют утверждённых owner/group/modes, permission check проходит повторно, active release всегда содержит matching Vite manifest/assets и rollback проверен. |
| TD-020 | PHP-FPM настроен на `pm.max_children=60` при четырёх физических ядрах, а OPcache `max_accelerated_files=10000` меньше примерно 13 934 PHP files репозитория/vendor. Категории: PHP/production/performance. | PHP-FPM, OPcache, web latency, memory | Capacity risk: чрезмерная concurrency может усиливать SQLite contention/context switching, а неполный OPcache inventory повышает cold latency; точный safe target без RSS/hit/restart evidence неизвестен. | Измерить child RSS, active/idle/max-active, queue/listen, CPU, SQLite busy rate и OPcache hit/miss/restart; менять один параметр за раз с canary/reload/rollback. | Production metrics and maintenance window; medium | Open. Значения только инвентаризированы. | Утверждённые FPM/OPcache budgets выполняются под representative load без queue growth, OOM, cache exhaustion или SQLite regression. |
| TD-025 | Полный `app:deployment-check` синхронно выполняет `PRAGMA quick_check`/FK на SQLite около `28,15` GiB; текущий read-only probe не завершился за 60 секунд, а fast activation profile отсутствует. Категории: deployment/database/production/availability. | Deployment preflight, SQLite integrity, activation and rollback | Availability/deployment risk: unbounded full integrity блокирует предсказуемое activation window; пропуск проверки без явного профиля создаёт ложный ready verdict. | Разделить `fast|full` profiles: fast сохраняет все lightweight correctness checks и явно помечает full integrity deferred; full выполняется только в approved writer-free maintenance/backup context без fake timeout success. | Master plan Task 8; medium | Open. Task 1 не запускала повторный full integrity и не меняла deployment command. | Fast profile имеет bounded measured duration и stable JSON contract; full profile успешно выполнен в approved window или честно `blocked`, а activation/rollback runbook различает оба режима. |

## Закрыто в Task 29 и последующих maintenance follow-ups

| ID | Исправление | Evidence / completion criterion |
| --- | --- | --- |
| TD-RES-001 | Три inline width progress indicators заменены одним application-owned native `<progress>` component. | В Blade нет `style=`; компонент имеет semantics, label/value text, theme-aware CSS и используется во всех трёх найденных местах. |
| TD-RES-002 | Пользовательские Russian literals в audited Form Requests и catalog user-card state перенесены в существующие `ru`/`en` catalogs. | Полный Request scan не содержит user-facing Cyrillic literals; recursive RU/EN key/placeholder parity прошла. |
| TD-RES-003 | Удалены две неиспользуемые Composer plugin permissions. | Плагины отсутствуют в lock; `composer.json` не pre-authorize их; lock-файл не переписан; validate/audit повторяются после изменения. |
| TD-RES-004 | Direct `getenv()`/`$_SERVER` fallback удалён из runtime service. | `IntegrationDoctor` читает только `config()`; optional home override и process-home default объявлены в `config/services.php`, `.env.example` и tooling/environment docs. |
| TD-RES-005 | Удалён default scaffold command `inspire`. | Repository-wide search подтвердил единственные definition/import в `routes/console.php`; portal/operator docs, data, scheduler, admin и deployment от него не зависели. |
| TD-RES-006 | Livewire generator больше не наследует package default `sfc`. | Effective `config('livewire.make_command')` фиксирует `type=class`, выключенные emoji/JS/CSS/test generation; package/lock/runtime components не менялись, Volt/SFC/MFC usage отсутствует. |
| TD-RES-007 | Закрыт `TD-012`: lease точной configured cache-warm connection/queue согласован с разрешённой длительностью `WarmCatalogCaches`, не расширяя lease остальных очередей. | Unit regression сохраняет прежнее обнаружение остановленного base pool и проверяет cache-warm до/после bounded grace; production worker после graceful recycle выполнял job более 136 секунд, heartbeat имел ещё 525 секунд TTL и health показывал реальный `degraded` backlog вместо ложного `failed`. Payload, key format, schema и очередь не менялись и не очищались. |

## Operational closures и оставшееся evidence

- `OP-001` — `resolved` 19.07.2026: effective runtime подтверждает `APP_ENV=production`, `APP_DEBUG=false` и cached config; выполнены config/route rebuild, graceful PHP-FPM reload и queue restart, production Debugbar routes отсутствуют.
- `OP-002` — schema/data outcome `resolved externally`: три comment/review migrations применены другим rollout в batch 31, пять additive administration migrations — в batches 32–33; read-only audit подтвердил корректные tombstone/index/FK и administration schema/seed invariants. Process evidence остаётся `unresolved`: Task 29 не наблюдала pre-migration backup evidence этих batches и не заявляет, что backup был создан. Следующий SQLite DDL запрещён без заранее проверенной копии и записанного evidence.
- `OP-003` — maintenance state `resolved` после завершения владельца окна: marker отсутствует, PHP-FPM активен, `/up` возвращает `200`, а Chromium ранее получил `/` `200`. Финальный curl `/` превысил 20-секундный timeout; это performance-риск `TD-011`, а не возврат maintenance mode. Task 29 не снимала maintenance mode во время активной чужой записи.

## Правило приоритета

Security vulnerability, privacy leak, data-loss risk, authorization/payment/legal bypass, private-cache leak, broken deployment, fake integration или подтверждённая blocking deprecation не могут быть перенесены сюда вместо исправления, когда безопасное исправление доступно в текущей задаче.
