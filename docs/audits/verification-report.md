# Verification report

Проверено: 15.07.2026 на `main` commit `70df36b` до новых modernization changes. Этот документ фиксирует baseline, а не заявляет завершение production rollout.

## Passed baseline

| Gate | Result |
| --- | --- |
| `php artisan test` | 826 tests; 815 passed, 11 skipped; 6751 assertions; 93.554 s |
| Focused browser CI contract | 2 tests, 41 assertions |
| `PLAYWRIGHT_RUNTIME_NAME=audit-baseline npm run test:browser` | 18/18 passed; 1.4 min |
| `./vendor/bin/pint --test --format agent` | pass |
| PHP syntax lint | exit 0 |
| Configured `vendor/bin/phpstan analyse` | 0 diagnostics |
| Full `app/` Larastan level 6 | 547 diagnostics; not suppressed, modernization backlog |
| `composer validate --strict` | pass; root/plugin warning only |
| `composer audit --locked` | 0 advisories |
| `npm audit --audit-level=high` | 0 vulnerabilities |
| `npm run build` | pass, Vite 8.1.4 in 2.51 s |

Build assets: app CSS 154.51 kB / 32.90 gzip; app JS 8.51/3.61; player JS 11.73/3.84; Plyr lazy 111.81/32.86; HLS light lazy 331.90/104.61.

## Production checks that are not green

| Check | Exact evidence | Status |
| --- | --- | --- |
| Environment | production, debug enabled | Blocker; environment owner action required |
| Laravel caches | config/events not cached; routes/views cached | Blocker after safe environment setup |
| Migrations | two pending | Blocker until writer stop + verified backup |
| Import lifecycle | Baseline 11 active runs/8037 pending/5670 claims; later read-only snapshot: 12 running sitemap runs, 4155 failed group finalizers, 793 page jobs, 9 preparation jobs and 1601 active groups | Code fixed; rollout/reconciliation blocker remains |
| Health truthfulness | New scoped check reports `failed`: 32821 pending, 4 reserved; cache-warm oldest age 151156 s and import oldest age 3162 s | Code verified; worker restart/cache-warm installation still pending |
| Deployment check | Instrumented full run: 24.45 s wall, SQLite quick/FK 23655 ms, remaining checks 0–303 ms; integrity pass; safe exit 1 for debug, 2 migrations and stale FTS | Finite but expensive; `duration_ms` verified; retain >=30 s operational budget |
| Cache worker | versioned unit exists in Git but no process/installed unit | Blocker for cache-warm readiness |
| Blade passive boundary | Baseline 41 `request()` + 1 `config()`; current strengthened scan has zero request/config/auth/gate/facade/container/application-static violations across 52 files | Code, full PHPUnit and 21/21 browser scenarios verified |
| Public storage | link absent | Verify need; not automatically a blocker |

## Browser evidence

The deterministic suite covers guest and authenticated portal, registration/login/private navigation, saved player progress/Continue Watching, catalog URL/filter state, route taxonomy removal, directory alphabet/search, title player shell, desktop/mobile geometry and accessibility assertions. Same-origin request failures and console errors fail the suite; expected aborted Livewire poll during navigation is recognized only by parsed poll method metadata.

## Safety

No `.env` edit, destructive database command, queue clear, failed-job payload dump, media download, new production dependency, branch/worktree or force push occurred in the baseline phase. Concurrent unrelated working-tree changes are excluded from these results until their own RED/GREEN cycle completes.

## Verified modernization increment

- Queue health RED/GREEN: separate backlog without a matching pool heartbeat fails; an idle loop records liveness for every listened queue.
- `QueueWorkerObservabilityTest` + `InfrastructureHealthCheckTest`: 7 passed, 49 assertions, включая expiry regression, которая запрещает memoized stale heartbeat.
- Deployment timing RED/GREEN: every JSON check has a non-negative integer `duration_ms`; text output also shows the duration without diagnostic details.
- CLI/HTTP policy regression: operational `app:health` exits 1 for `degraded`, while `/health/ready` remains HTTP 200 when critical traffic transports are ready; response remains private/no-store and path-free.
- Combined preflight/queue/health increment: 12 passed, 88 assertions; changed-scope Larastan 0; targeted Pint pass.
- Live read-only `app:health --json`: `degraded`, `ready=true`, exit 1; queue component `failed`, 32325 pending across four named queues and no secret/path leakage. Current long-running workers still run pre-rollout code, so missing scoped heartbeat remains expected until restart.
- Import lifecycle RED/GREEN: admin/CLI/repeated dispatch shares one atomic active global run while targeted title refresh remains independent; rejected dispatch becomes a sanitized terminal failure.
- Finalizer RED/GREEN: incomplete siblings/live claims return without polling release; terminal page/group completion signals deduplicated `ShouldBeUniqueUntilProcessing` finalizers; unique ten-minute watchdog restores lost signals in bounded batches.
- Importer increment: 52 tests, 266 assertions; changed model/job/service Larastan 0 with `--memory-limit=1G`; targeted Pint pass; `schedule:list` confirms `*/10 * * * * seasonvar-import-finalization-watchdog`.
- Passive Blade increment: shared typed navigation state removes route/auth/config decisions from templates; 42 shell/auth/Blade tests / 339 assertions, broader 120/1126 catalog set and full 840/6882 suite pass; `view:cache`, Vite build and changed-scope Larastan pass. The 21/21 desktop/mobile/tablet browser matrix passes. Build output: app CSS 156.01/33.09 gzip, app JS 8.51/3.61; lazy player chunks unchanged.
- Layout/SEO increment: unreachable generated matrices removed after repository-wide flag/consumer tracing; explicit contract and prepared `Js::encode()` JSON-LD reduce AppLayoutData/layout from 1,928/783 to 411/96 lines. Same 100-iteration rich-payload harness improved median 23.894→0.536 ms and p95 25.323→0.834 ms; focused 130/1,198, full 848/6,928, changed-scope Larastan 0, compiled views, production build and 21/21 browser matrix pass. Build output: app CSS 155.97/33.09 gzip and app JS 8.51/3.61; player chunks unchanged.

## Task 10 collection verification (no automated tests)

The task explicitly prohibited creating or running automated tests, so its acceptance used static inspection, isolated schema/query exercises and manual Chromium flows only. No PHPUnit, Pest or Playwright test runner was invoked for this increment.

| Gate | Fresh evidence |
| --- | --- |
| Canonical routes | Isolated `route:list` exposes 14 web/API/sitemap/admin/localized collection routes plus the four documented `/lists`, `/my/lists` and `/selections` compatibility routes; owner mutations remain Livewire POST actions behind authenticated policy checks. |
| Domain static analysis | Collection DTOs/enums/controllers/requests/resources/Livewire/models/policy/services pass focused Larastan with 0 diagnostics. The final run caught and fixed singular taxonomy relation names before acceptance. |
| Syntax and formatting | Every changed or untracked PHP/Blade file passes `php -l`; `pint --dirty --format agent` exits 0. |
| Translation and template boundaries | `lang/en/collections.php` and `lang/ru/collections.php` each contain 230 leaf keys with zero missing keys or named-placeholder mismatches. Collection Blade contains no `@php`, inline style/script, model query, facade, `auth()`, `config()` or `request()` call; collection JavaScript contains no console/debug/TODO marker. |
| API contract | OpenAPI 3.1 JSON parses and declares the public collection directory, collection item page and title-to-collections endpoints. API pagination uses the conventional `page` key and resources expose opaque owner/collection IDs only. |
| Disposable schema | A fresh isolated SQLite applied the entire migration chain through `2026_07_15_235100`; manual collection queries for genre, country and status filters plus all option groups returned normally. Collection migrations then rolled back successfully: zero `catalog_collection%` tables, zero collection migration rows and no `users.public_id` remained. The wider historical rollback subsequently stopped at the unrelated released importer migration `2026_07_09_204238`; Task 10 does not rewrite that old migration. |
| Pre-migration compatibility | A separate empty SQLite database with an isolated config-cache path returned `CatalogCollectionSchema::available=false`, an empty directory paginator and empty account export; account cleanup was a no-op, direct slug resolution failed closed with `404`, and collection create failed closed with `503`. A second disposable schema created a user before the additive column with the legacy INSERT shape (`public_id=null` in memory, no SQL error), then added the column and created another user with a valid UUID. No absent collection/user column query escaped. |
| Query plans and merge | Representative SQLite plans use the owner/public ordering, item manual/title lookup, translation locale/name and report queue indexes; the import-driven collection lookup uses the covering `(catalog_title_id,catalog_collection_id)` index plus collection primary key, while public counts and membership use grouped/existence queries rather than per-card reads. A disposable two-collection title merge retained the earliest timestamp/lowest position, moved the duplicate-only row, normalized both positions and left zero duplicate-title memberships through bounded `eachById(500)`. |
| Mutation and recovery invariants | A fresh full-schema service exercise returned hydrated create version `1`; an exact current-version save kept domain/cache versions unchanged, while one rename advanced each exactly once and retained one historical slug. A fulfilled stale-version edit retry, duplicate delete/restore and repeated moderation kept domain versions stable and each issued one repair cache bump. A separate disposable membership exercise confirmed add/remove return `true,false` across first delivery/retry, each material change advances content once, batch retry returns an empty changed set, exact full-order retry leaves content unchanged, empty cover removal leaves both domain versions unchanged, and every fulfilled retry advances only the bounded global/scoped cache generation. A forced missing-storage-disk cleanup after cover removal was safely reported without escaping: cover/content and cache generations each advanced once and the authoritative path became null. The admin query selected an otherwise report-free approved public editorial record, excluded a private user record, and the authorized feature action kept the editorial row in the queue after setting `is_featured=true`. User content locale stayed null, approved editorial private→public set publication time, and scheduled prune removed one 31-day tombstone while preserving a recent deletion. |
| Editorial locale SEO | A disposable approved public collection with one guest-visible title and real `ru`/`en` translation rows rendered distinct original display text, indexable unprefixed Russian and `/en/` canonicals, and the same reciprocal `ru`, `en`, `x-default` alternate map from both locales. An empty editorial collection remained noindex with no alternates, preserving the thin-content rule. |
| Framework compilation | Isolated configuration and routes cache successfully; all Blade templates compile through `view:cache`. |
| Production assets | Vite 8.1.4 builds successfully: app CSS 167.03 kB / 34.94 gzip and app JS 14.59 / 5.27 gzip; lazy player/Plyr/HLS chunks remain split. |
| Manual browser acceptance | Isolated Chromium covered guest/owner/second-user authorization; create/edit/sanitize/rename/history redirect; private/public/unlisted/editorial pages; staged multi-membership and create-and-add; duplicate apply; remove/re-add; manual move; soft-delete/restore; filters/no-results/pagination URL back-forward; report/deduplication; directory/profile eligibility; canonical/robots/JSON-LD/hreflang; desktop and 390×844 dialog/focus/overflow. Console, page and request failure counts were zero. |
| Cache/privacy evidence | Private pages and covers returned `private, no-store` and unauthorized requests returned safe 404s without name/count/email/internal-ID leakage. Public directory/profile variants use the collection version domain; the version advanced narrowly from 8 to 9 after an isolated-process namespace collision, with no global cache flush. A disposable importer-cache exercise left Collections/global TitleDetail unchanged for an unlinked title while advancing only `title:2`; an update to a title present in two approved public collections advanced Collections, Homepage, Sitemap, Recommendations, API and both exact collection scopes once, advanced only `title:1`, and left global TitleDetail unchanged. |

## Task 11 tag verification (no automated tests)

Task 11 explicitly prohibited creating or running automated tests. Acceptance therefore used code/route/schema/query/privacy/SEO inspection, disposable SQLite rehearsals and direct managed-Chromium smoke only. No PHPUnit, Pest or Playwright test runner was invoked for this increment.

| Gate | Fresh evidence |
| --- | --- |
| Canonical routes and authorization | Isolated `route:list -v` exposes `/tags`, canonical `/titles/{type}/{taxonomy}`, compatibility `/tags/{value}` and `/tag/{taxonomy}`, `/admin/tags`, owner-only `/library/tags/manage`, public API reads and verified-email/write-ability private API mutations. Admin remains behind `manage-catalog`; personal endpoints remain authenticated, ability-scoped and owner-resolved server-side. |
| Disposable schema and indexes | A fresh isolated SQLite applied the full repository chain through `2026_07_15_235100`, including `230000`, archive preservation, eligibility-index replacement, normalization repair, provider-provenance backfill and exact-duplicate reconciliation. `PRAGMA foreign_key_check` returned no rows. `tags_normalized_name_hash_unique`, translation/pivot uniqueness and the equality-first `tags_public_eligibility_idx` exist; `EXPLAIN QUERY PLAN` selected the latter for approved/public/non-archived/non-merged reads. |
| Normalization and translations | Runtime `TagNormalizationService::comparison()` and the additive repair migration returned identical results for seven Unicode/entity/NBSP/full-width/dash/punctuation/control samples. Russian and English tag catalogs contain the same 181 leaf keys and the same unique named-placeholder sets; original personal labels remain database UGC rather than locale-file content. |
| Personal-tag domain exercise | A copy of the fresh disposable schema exercised the real service with two verified owners and one visible title. Creation assigned the owner server-side and returned hydrated version `1`; normalized duplicates were idempotent only within one owner; cross-owner assignment/edit failed; batch assignment and removal were idempotent with one pivot; edit preserved UUID/database identity and stripped markup; soft delete/restore preserved the assignment; the global `tags` count did not change. |
| Static analysis and formatting | Focused Larastan over tag DTOs/enums/controllers/requests/resources/Livewire/models/policies/services, catalog/import/account integrations and all six tag migrations returned 0 diagnostics. Final syntax lint covered 1,033 repository PHP source/test files plus 227 compiled views with 0 errors. Pint over the final Task 11 PHP change set passed. |
| Template and UI boundary | Tag/catalog Blade scan found no Volt, `@php`, inline style/script, model/facade/query/container call, TODO/debug/console marker or forbidden nested scroll/truncation utility. Meaningful labels/counts retain readable contrast, controls use 44 px targets, long text wraps and primary writes use the shared emerald action palette. |
| Framework/API/assets | Isolated configuration, routes and 141 Blade templates cached successfully. OpenAPI JSON parses. `project:docs-refresh --check` is current after the managed `SOURCE_PARITY` refresh. Vite 8.1.4 built app CSS 165.14 kB / 34.59 gzip and app JS 15.29 / 5.41 gzip while player/Plyr/HLS stayed split. |
| Manual public browser smoke | Managed Chromium (CLI fallback because system Chrome was absent) returned 200 for `/tags`, `/titles/tag/subtitry` and `/api/v1/tags?q=sub`; `/tag/subtitry`, `/tags/subtitry` and uppercase canonical input returned 301 to `/titles/tag/subtitry`. Desktop `1440×1200` and mobile `390×844` each had one Russian `h1`, one `main`, no horizontal overflow and zero console/page/local-request failures. API output was JSON with public cache headers. |
| Canonical filter interaction | The canonical tag page remained exactly `/titles/tag/subtitry` after Livewire hydration. Selecting `dorama` produced only `?tag[0]=dorama`; removing the checked route tag moved to `/titles?tag[0]=dorama`. This proves locked route identity is not serialized twice and independent filter history survives route-context removal. |
| Privacy/cache/SEO inspection | Personal labels/assignments are owner-scoped, use opaque UUIDs and private no-store resources, and never enter global search, recommendations, counts, sitemap, public HTML or shared tag snapshots. Public reads require approved/public/non-internal/non-archived canonical tag plus a guest-visible title. Alias/history/merge/case inputs redirect; empty/ineligible tags fail closed. Real localized content supplies metadata, while absent locale-prefixed routes produce no false `hreflang`. |
| Import/merge compatibility | Static and disposable reconciliation inspection confirms repeated provider observations reuse mappings/assignments, rejected mappings and editorial suppressions are preserved, stale pivots detach only without another current source, and exact duplicate merge moves/deduplicates title assignments, translations, aliases, slug history, provider provenance and reverse synonyms while preserving source→canonical history. A separate two-owner title-merge fixture retained five distinct personal assignments once, emptied the duplicate title pivots and compacted each owner's positions independently to `0..n-1`. Existing tag IDs, current slugs, source URLs and pivot rows remain the rollback baseline. |
| Production safety | The configured SQLite database was not used for final rehearsals. The earlier cached-config mistake and its additive batch are disclosed in `docs/MAINTENANCE_LOG.md`; no destructive rollback was attempted. Remaining Task 11 follow-up migrations on that database still require the normal backup/writer-stop/deploy workflow and are not represented as a completed production rollout. |

## Task 12 comment and discussion verification (no automated tests)

Task 12 explicitly prohibited creating or running automated tests. Acceptance therefore used repository-wide documentation and code inspection, focused static diagnostics, disposable SQLite exercises and direct managed-Chromium interaction. No PHPUnit, Pest or Playwright test runner was invoked for this increment.

| Gate | Fresh evidence |
| --- | --- |
| Canonical architecture and routes | The audit read all 212 project Markdown files and found no pre-existing portal comment/reply/reaction/report tables or competing implementation; imported `catalog_title_reviews` remains a separate provider-review domain. An uncached route registry contains 161 unique names and exposes canonical/localized `comments.show`, private `profile.discussions` and `notifications.index`, and gate-protected `admin.comments`; all mutations remain Livewire POST actions with CSRF and policy/action reauthorization. |
| Syntax, formatting and static analysis | Syntax lint passed for all 84 Task 12 PHP files. Focused Pint passed, and focused Larastan over the comment domain, direct integrations and final index migration returned 0 diagnostics with a 512 MB analysis budget. No automated test command was run. |
| Disposable schema and rollback | A fresh isolated SQLite applied the complete repository migration chain through `2026_07_15_235200`. The eight discussion tables, 11 required query-supporting composites and all foreign keys were present; `PRAGMA foreign_key_check` returned no rows. The four core discussion migrations plus `235200` completed a direct clean down/up cycle. The configured production-style database was not migrated. |
| Query plans and bounded reads | SQLite plans selected the exact target/status/order index and the new covering `(blocker_id,id)` / `(muter_id,id)` profile-list indexes. Top-level comments remain 15-per-page, replies are progressively bounded, moderation context is root plus at most 20 replies, report cards preview five rows and one moderation decision resolves at most 100 reports. Current reactions, relationship overlays and reply counts are grouped rather than queried once per item. |
| Translation and template boundaries | `lang/ru/comments.php` and `lang/en/comments.php` each contain 234 leaf keys with identical paths, order and named placeholders; all 48 enum label keys resolve in both locales and runtime plural checks use native Russian three-form rules. Discussion Blade/JavaScript scans found no Volt, `@php`, inline style/business script, model/facade/database call, raw comment output, console/debug/TODO marker or hardcoded action label. |
| Framework and assets | All 141 Blade templates compiled. Vite 8.1.4 built successfully with app CSS 165.19 kB and app JS 15.29 kB while player/Plyr/HLS remained split. Strict Composer validation was structurally valid with only the existing root/plugin warnings; `composer audit` returned no advisories and `npm audit` returned zero vulnerabilities. `project:docs-refresh --check` was current before this final documentation update and is rerun at handoff. |
| Author lifecycle browser flow | In an isolated Russian Chromium session, a verified author created a 634-character whole-comment spoiler, revealed and re-hid it explicitly, edited it to 945 characters, expanded/collapsed the long body, soft-deleted it to a reachable tombstone and restored it. Stable database ID `1`, `/comments/1` and `#comment-1` survived every mutation; unrevealed spoiler text was absent from the accessibility snapshot/HTML presentation. |
| Reply, reaction, report and relationship browser flow | A second verified user followed the direct redirect to the canonical title context, changed the desired reaction `up → down → none` without duplicate rows, created a reply with structural/logical parent `1`, and submitted one `spam` report. Mute and bilateral block each hid the author's body through a neutral placeholder while preserving thread structure; private profile lists exposed only the viewer's relationship and both unmute/unblock restored the normal view. |
| Moderation and restriction browser flow | The protected queue displayed the full spoiler only to the moderator, private report detail and bounded thread context. A reversible `published → hidden → published` decision made the public direct URL return a safe 404 while hidden. The report resolved without exposing the reporter. A one-day comment-only restriction blocked the second user's composer with a localized reason/expiry and was then revoked immediately; self-restriction by the moderator was rejected. Permanent restriction controls use the same authorized stable-enum action without a speculative second ban system. |
| Accessibility and responsive browser flow | Spoiler, long-body, reply, edit, delete/restore, reactions, report dialog, relationship and moderation controls were keyboard-addressable and localized. Focus returned after replacement actions, direct focus highlighted the stable comment, and reduced-motion behavior remained in the Vite module. At 390×844, document width equalled viewport width, the discussion measured 366 px and the flattened reply retained 328 px of readable width with no horizontal overflow. |
| Notifications, privacy and final browser health | Reply, reaction, moderation and report-resolution notification payloads contained stable IDs/status only and zero excerpt/body bytes. The author inbox showed safe activity without spoiler preview. Fresh reader/admin sessions ended with zero console errors and every observed Livewire update returned HTTP 200. Account export/deletion, target retirement/merge, cache separation, hidden direct links and moderator-only evidence were also re-inspected statically. |
| Final disposable state | The fixture ended with two published comments, one stable root/reply relation, no reaction row, one resolved report and one revoked temporary restriction; comment foreign keys remained valid. The temporary server, browser sessions and disposable database were removed after evidence capture. |

### Task 12 post-profile integration verification (no automated tests)

The 16.07.2026 follow-up re-read the source-owned Markdown corpus and traced the later Task 14 public-profile projection back through profile privacy, title visibility and the canonical comment query/presenter. No PHPUnit, Pest or Playwright test runner was invoked.

| Gate | Fresh evidence |
| --- | --- |
| Canonical projection | The duplicate profile-owned DTO/query/count was removed. `PublicUserProfileQuery` and `PublicUserProfilePresenter` now delegate rows and matching counts to `CommentProfileQuery`; `CommentPresenter` alone creates the public activity DTO, omits every spoiler excerpt and emits canonical title/direct URLs. Profile code retains section privacy and paginator configuration only. |
| Static/runtime boundary | Exact-file PHP syntax and Pint passed; focused Larastan returned 0 diagnostics. Booted-container probes resolved all injected services, confirmed the shared empty-page/count result on the current read-only fixture and verified canonical display title, bounded Unicode excerpt, spoiler omission and route generation without a database write. Uncached route/model inspection retained canonical/localized comment and profile routes and the existing comment policy/schema. |
| Translation/templates/assets | Russian and English comment catalogs remain at 234 identical leaf keys with no missing paths. All Blade templates compiled and Vite 8.1.4 completed the production build; no Blade, translation, JavaScript, CSS or route change was required. |
| Browser smoke | Managed Chromium used a production-host-to-loopback resolver because `TrustHosts` correctly rejects raw `127.0.0.1`. The public profile returned 200 at `1440×1200` and `390×844`, rendered one `main`, had no horizontal overflow and produced zero console/page/same-origin request failures. The fixture keeps comments private/empty, so no inactive section was invented; an unavailable direct comment returned a safe 404 without internal details. |

## Task 13 post-profile integration verification (no automated tests)

Task 13 explicitly prohibited creating or running automated tests. The 16.07.2026 follow-up therefore used the already-completed Task 13 acceptance record plus a full Markdown/code integration re-audit, focused static analysis, read-only production-style schema queries and booted-container probes. No PHPUnit, Pest or Playwright test runner was invoked.

| Gate | Fresh evidence |
| --- | --- |
| Canonical routes and schema | Uncached route inspection exposes only the existing provider API, stable direct review link, private self history and gated moderation route. All three review migrations report `Ran`; `ReviewSchema` reports legacy/community/write/notification capabilities ready. No route, table, migration or cache namespace was added. |
| Data integrity | Read-only queries over 73 131 reviews found zero duplicate non-null ownership/submission keys, duplicate `(review,user)` votes, orphan title targets or unknown moderation status values. Existing unique keys and additive schema remain sufficient; no reconciliation or production write was performed. |
| Public-profile privacy fix | The re-audit found Task 14 had repeated review visibility/projection inside `PublicUserProfileQuery` and omitted only spoiler body, not spoiler title. Public profile rows/counts now delegate to `CatalogTitleReviewQuery`/`ReviewPresenter`; a booted-container probe confirmed both spoiler title and excerpt are `null`, stable `/reviews/{id}` remains intact and the title URL uses its canonical slug. Comments/watch profile rows were moved to typed DTOs only to satisfy the same paginator boundary; their content policy is unchanged. |
| Localization and template boundaries | Russian and English review catalogs each contain 223 identical leaf paths and named-placeholder sets. A real HTTP-kernel request returned localized `422` pagination errors for both `ru` and `en`. Review/profile Blade scans found no Volt, `@php`, inline style/business script, model/facade/database/container call or raw spoiler text path. |
| Static verification | Focused Larastan over the changed PHP integration returned zero diagnostics; PHP syntax, Pint and diff whitespace checks passed. Routes, migration status, policy/query/cache/SEO/sitemap boundaries and directly related unchanged files were re-inspected. All Blade templates compiled, managed docs were current and Vite 8.1.4 built 17 modules successfully; no automated test suite was executed. |
| Read-only browser smoke | Managed Chromium followed a real public `/reviews/{id}` redirect into the canonical title page at desktop `1440×1200` and mobile `390×844`. Both returned 200, rendered one H1 and the localized review heading, loaded seven bounded cards, found and visually highlighted the requested stable review, had no horizontal overflow, and ended with zero console errors, page errors or failed same-origin requests. No form action or data mutation was performed. |

## Task 16 account-settings verification (no automated tests)

Task 16 explicitly prohibited creating or running automated tests. Acceptance therefore used a full tracked-Markdown audit, static analysis, isolated SQLite migration rehearsal, direct managed-Chromium interaction and production asset compilation; no PHPUnit, Pest or Playwright test runner/spec was invoked.

| Gate | Fresh evidence |
| --- | --- |
| Documentation and architecture audit | All 189 tracked Markdown files (39,377 lines after the update) were inventoried/read and the relevant route/model/service/schema/player/profile/privacy/notification/session/export/delete/cache/localization contracts were traced. No prior general preference table, premium/billing/entitlement model, OAuth/external identity, public username/avatar/cover/biography store, media-language track registry, extra player style or configurable continue-watching mode exists; those capabilities remain documented limitations rather than fake controls. |
| Canonical routes and private boundary | An isolated route cache exposes exactly `settings.index`, `localized.settings.index` and POST-only `settings.preferences.migrate`. All three require `auth`, `auth.session` and `PrivateAccountResponse`; the merge adds `12/min` throttle and localized routes add `SetInterfaceLocale`. Normal settings URLs contain no user ID/private value, sections are enum-allowlisted, and rendered settings return `private,no-store`, `X-Robots-Tag: noindex,nofollow`, no social JSON-LD and no sitemap membership. |
| Static analysis, syntax and formatting | All 49 changed PHP files pass `php -l`; focused Larastan over every changed app/bootstrap/config/migration/route file returns 0 diagnostics; final `pint --dirty --format agent` passes. Diff whitespace check is clean. Settings/profile/security Blade scans contain no Volt, `@php`, raw output, inline style/script, model/facade/container/query call, debug/TODO or console marker. |
| Translation and accessibility structure | `lang/ru/settings.php` and `lang/en/settings.php` each contain 223 leaf keys with zero missing paths or named-placeholder mismatches. One active section is rendered, native labelled controls retain server state, live status/error regions and confirmations are localized, and the normal links preserve back/forward. Browser DOM inspection found one H1, one `aria-current`, zero unlabeled settings controls and no horizontal overflow at `390×844`; desktop `1440×1200` and mobile screenshots were visually inspected. |
| Disposable schema and rollback | A fresh temporary SQLite database applied the entire repository migration chain through `2026_07_16_000000`. `user_account_settings` had one `user_id` primary/FK, `ON DELETE CASCADE`, all nullable explicit-choice columns and no redundant index. The Task 16 path rollback removed only that table/migration row while leaving `users`; the configured production-style database was never migrated or rolled back. |
| Preference storage, validation and precedence | Static/service inspection confirms nullable explicit database fields, material-only monotonic versioning, config fallback, URL locale precedence, account-scoped local state and device-only immediate volume behavior. Account/service/request allowlists reject arbitrary keys/JSON paths, invalid locale/IANA timezone, out-of-range volume, unsupported speeds and non-eligible quality/variant codes; translated labels and source URLs are never stored. Anonymous merge is throttled, idempotent, fills only null fields and preserves legacy Plyr storage. |
| Player and collection integration | The existing `CatalogTitlePlayer`/resolver/Plyr/HLS path remains singular: explicit URL selection wins, account stable quality/variant is fallback, unavailable saved values remain without exposing URLs, zero-volume remains valid, and debounced device writes avoid slider request storms. Reset touches only playback columns. Browser save confirmed autoplay, volume `42`, speed `1.25`, subtitles and opaque owner-scoped local storage. Collection default save confirmed `unlisted`; service/dashboard code applies it only to later creates and never rewrites existing collection rows. |
| Notifications, profile and lifecycle | Browser save changed the actual comment/review in-portal matrix through the canonical actions; delivery services still read those dedicated tables and missing rows remain non-persisted defaults. Profile retains one `name|email` allowlisted service with plain-text validation; exact history/progress/library remain private. Export includes the resolved non-secret settings summary, while password/tokens/session IDs are excluded and deletion remains the existing password-confirmed transactional service/FK cascade. |
| Sessions and security | Static inspection confirms database session summaries select only ID/user-agent/activity internally, emit only HMAC action tokens, parsed device labels and localized times, cap reads/revocation candidates at 20 and never expose raw ID/payload/user-agent/IP/cookie. Revoke and logout-other are password-confirmed, rate-limited, owner-scoped and exclude/preserve current session; generic settings mutations repeat a formal self-context Gate. |
| Browser flow and failure handling | Managed Chromium was used because Playwright CLI required an unavailable system Chrome. The isolated seeded admin flow covered default/localized settings, locale save and same-section English redirect, IANA timezone, reduced motion, playback, notifications, collection default, back/forward, desktop/mobile geometry and local storage with no console/page/local-asset failures. A second seeded account sent an anonymous merge, received 204, loaded migrated autoplay/speed and removed cross-device fields from local storage. An intentionally immediate full navigation caused the PHP built-in server/Playwright transport to report that already-acknowledged nonessential fetch as `ERR_ABORTED`; login and authoritative server state remained correct, demonstrating the documented non-blocking/idempotent failure boundary rather than a production dependency. |
| Framework/docs/assets | Temporary config, route and all Blade caches compile. `project:docs-refresh --check` reports current managed blocks. Vite 8.1.4 builds 16 modules successfully: app CSS 165.42 kB / 34.60 gzip, app JS 20.84 / 6.95 gzip, player JS 14.34 / 4.53 gzip, with Plyr/HLS remaining split. |
