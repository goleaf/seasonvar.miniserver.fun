# Changelog

## 2026-07-16

- Made `CACHE_WARMING_ENABLED=false` a complete automatic-warming kill switch: catalogue mutations and the ten-minute scheduler now both skip dispatch while disabled, while the manual command and enabled behavior remain unchanged. Production verification across the 10:00 and 10:10 scheduler cycles kept `cache-warm-v2` empty and added no failed jobs; no cache flush, queue cleanup, retry, or data mutation was used.
- Hardened post-authentication redirect validation against percent-encoded authentication-route and browser dot-segment normalization bypasses while preserving valid encoded UTF-8 catalogue paths and same-origin query destinations.
- Added independent HMAC-scoped login rate limits for normalized identifier and network alongside the existing identifier/network-pair budget, closing distributed account brute-force and single-network credential-spraying bypasses consistently across Livewire and Sanctum authentication without storing raw email or IP values.
- Added matching HMAC-scoped pair, normalized-identifier, and network budgets to browser and mobile password recovery/reset flows, preventing distributed recovery-email flooding and single-network recipient spraying while retaining generic account-enumeration responses and clearing only successful reset identity buckets.
- Made Sanctum mobile token refresh single-use under concurrency by owner-scoping and locking the persisted token before revocation and replacement; a stale or simultaneous replay now returns the existing generic unauthenticated response and cannot mint a second active token, while device name, abilities, expiry policy, API shape, routes, and schema remain unchanged.
- Made authenticated password, email, and account-deletion mutations validate the current password against a freshly locked user row inside the same transaction as the sensitive write, preventing a stale concurrent request with an older password from replacing a newer credential, changing the login email, or deleting the account while preserving the existing API, session, and schema contracts.
- Fixed cooperative shutdown of the canonical synchronous Seasonvar importer without adding another command, worker, or status model. `SIGINT`/`SIGTERM` now use Laravel's signal registry, finish the current page-level HTTP/database boundary, refuse the next page and later maintenance phases, persist accurate selected counters and stop phase events, and terminate the run as `cancelled` instead of falsely reporting a partial batch as completed. Existing queued cancellation, importer identity, locks, source records, media, and retries remain unchanged.
- Added the portal's first canonical missing-content request domain without converting comments, reports, reviews, support data, or importer records into competing tickets. Stable UUID requests now cover serials, seasons, episodes, translations, subtitles, quality upgrades, metadata and episode-list corrections, restoration, and other content needs; server-side catalogue/exact/probable duplicate checks, safe evidence/provider validation, idempotent requester votes/follows, centralized status history, clarification, moderation, merge, rejection, partial/full completion, preference-aware notifications, account lifecycle hooks, scoped cache invalidation, localized public/private/admin Livewire pages, SEO/sitemap rules, and a verified handoff to the existing Seasonvar importer are included. The additive eight-table migration preserves every existing portal table, route, cache key, and API contract; rolling schema guards keep the portal usable until deployment migration and cache rebuild complete.
- Hardened the existing Laravel web/Sanctum authentication system without replacing user identity or introducing a schema migration: canonical email normalization, shared password policy and mobile rehashing, configuration-controlled registration, privacy-safe layered limiter/audit fingerprints, internal-only redirect resolution, localized RU/EN web/API validation and verification/reset mail, current-password email/session/device controls, sensitive Livewire-state cleanup, and targeted registration/login/verification/recovery/reset/session/token audit events now share typed services. Canonical routes and all user/profile/library/progress/collection/comment/review/moderation data remain unchanged; social providers, automatic merging, magic links and MFA remain explicitly unsupported rather than appearing as dead controls. Static route/config/schema/security/translation inspection, Pint, view compilation, Vite build and read-only desktop/mobile Chromium smoke passed without creating or running automated tests.

### Recommendation and discovery architecture

- Replaced global season/episode availability ID-list materialization inside the existing canonical media scope with exact correlated primary-key checks. Publication, audience, availability windows, soft deletion, media health/playback, authenticated behavior, filters, ranking, result codes and cache identity remain unchanged; complete public and bounded member result hashes stayed exact. SQLite now uses the existing title-keyed media index plus primary keys, with no migration, summary table or background synchronization owner.
- Reworked `top_rated` candidate ranking to start from its authoritative source instead of evaluating correlated rating and vote subqueries for every visible title. Portal ratings are grouped once and Kinopoisk/IMDb use their existing unique indexed provider rows; minimum-vote rules, source isolation, visibility, filters, exclusions, deterministic ordering, result codes and public cache identity are unchanged. Read-only public result hashes remained exact, while isolated local candidate observations fell from 1.412s/1.807s/1.721s to 57ms/1.349s/954ms without a migration or stored summary.
- Hardened the cold `recently_updated` discovery path without changing recommendation identity or adding mandatory infrastructure: bounded publication-event windows replace the full historical aggregate, one focused episode release index supports deterministic ordering, and canonical visibility is applied in bulk. On the current SQLite catalogue the isolated candidate query fell from 9.623s to 1.748s while still returning 180 unique eligible titles; the existing optional warmer now also covers only the five stable indexable public discovery defaults, never personal, random, filtered, or private state.
- Added one canonical recommendation service for personalized, similar, related, editorial, trending, popular, top-rated, recently added/updated, upcoming, and indexed random discovery.
- Added real personalization from meaningful viewing progress, watchlist/status, ratings, owned collections, and private personal-tag assignments, with honest anonymous and cold-start public fallbacks.
- Added centralized visibility, watchability, blacklist/not-interested exclusions, undo and library restore, completed/watching/dropped handling, availability reranking, bounded repeat suppression, and genre/actor/franchise diversity.
- Added explicit typed sequel, prequel, spin-off, remake, universe, companion, and provider relations with authorized editorial controls, inverse/cycle validation, provider provenance, importer protection, and canonical-title merge reconciliation.
- Separated recent semantic trending activity from lifetime popularity, added one-source minimum-vote top-rated ranking, meaningful publication/update/upcoming signals, and random indexed-range probes without catalogue-wide random sorting.
- Added localized responsive discovery routes and legacy redirects, taxonomy/media filters, truthful explanations, loading/empty/error/accessibility states, homepage/title/search/library integration, public-only API compatibility, and public/private SEO and sitemap rules.
- Added additive recommendation feedback/watch-status fields, relation storage and focused SQLite indexes; preserved all existing similarity rows, user state, editorial collections, route names, API fields, cache domain, and importer command.
- Documented cache dimensions/invalidation, privacy and authorization, query limits and cold diagnostics, deployment/rollback, unsupported product models, and the manual no-test verification contract.

### Размер видеофайла и авторизованное скачивание

- Добавлено безопасное определение точного размера прямого внешнего видео через `HEAD` и минимальный `Range: bytes=0-0` без чтения полного body; nullable byte metadata, источник, статус, timestamp, HTTP status и очищенная ошибка сохраняются в `licensed_media`.
- Новый/изменённый media проверяется единым importer action, неизменённый соблюдает TTL/retry, HLS помечается unsupported. Синхронный и queued importer сохраняют атомарные counters/events; `seasonvar:import --refresh-media-sizes` выполняет ограниченный backfill существующих записей.
- Legacy size backlog теперь автоматически продвигается каждые 10 минут консервативной config-backed пачкой через ту же публичную команду, общие Redis/import locks и persistent progress. Size-only запуск не сбрасывает весь catalog cache, а результат HEAD/Range сохраняется conditional update только пока title/effective URL/format не изменились, поэтому запоздалый ответ не может привязать старый размер к новому источнику.
- Import inspection и download-time correction теперь используют один typed metadata writer: общий pre-HTTP source snapshot, conditional update, in-memory sync и material-only title cache invalidation исключают расхождение двух optimistic persistence путей без изменения schema, route или streaming contract.
- Добавлена глобальная наблюдаемость legacy size backlog без нового importer workflow: pipeline, `seasonvar:import --status` и `/admin/imports` используют одну direct-file eligibility/TTL boundary. Typed snapshot одним SQL aggregate показывает eligible/checked/pending/due/terminal totals, coverage и exact/human known bytes; operational tiered cache с пятнадцатиминутной свежестью, видимым capture time и bounded stale fallback не допускает полного scan 873k media при каждом Livewire poll, не содержит URL и не выполняет сетевых проверок во время render.
- Зарегистрированным пользователям добавлен scoped policy-protected download прямого файла с SSRF/DNS pinning, private/no-store attachment headers, безопасным стабильным filename, rate limiter и корректным single-range resume. Поток идёт bounded chunks без сохранения видео на сервере; гость и недоступные media получают отказ.
- Player Livewire показывает сохранённый человекочитаемый размер, локальную download icon/action, login CTA для гостя и stream-only состояние для HLS на русском и английском языках.

- Closed a cross-mode Seasonvar lifecycle gap that allowed one synchronous and one queued global sitemap run to remain live together. The existing short global start lock now checks active sitemap rows across both execution modes; sync CLI and the legacy wrapper reserve their lifecycle row through the same coordinator and pass it into the pipeline instead of creating a duplicate. Repeated sync/queued/admin starts reuse the active run without dispatch, while targeted URL import, inventory and status remain independent. The change does not stop or rewrite existing runs and introduces no migration.
- Added a bounded read-only failed-finalizer reconciliation audit without creating a queue-management workflow. Deployment preflight now materializes only allowlisted job names, a 256-byte exception prefix and timestamps for stable class/reason/age buckets; `app:failed-job-audit` separately validates exact finalizer envelopes up to 16 KiB with a non-instantiating serialized-value parser, maps positive scalar target IDs to grouped current run/group/claim state, and emits only safe disposition codes plus bounded ID samples. It has no retry, forget, clear or dispatch options. The live audit mapped all 4,155 historical group-finalizer rows to terminal groups; total failed jobs remained 6,714 before/after, so no historical envelope was retried or removed and no schema change was introduced.
- Closed the finalizer's permanently-impossible group gap without introducing a competing status machine. A bounded watchdog now also wakes active groups older than the full retry/claim window plus 300 seconds; the finalizer transaction rechecks the active run, freshness and live claims before assigning an allowlisted terminal reason. Expired `queued/preparing` rows can preserve valid prepared siblings as `partial`, while empty, structurally incomplete, wholly unusable or exhausted-finalizer groups become `failed` with stable codes and safe Russian explanations. An additive nullable reason column and `(status, updated_at, id)` watchdog index passed isolated SQLite up/down rehearsal; current production aggregates were inspected read-only and no queue row, claim or catalog record was changed.
- Completed explicit retry and terminal-failure contract coverage for all eight Seasonvar importer jobs. The regression matrix locks the attempt-bounded versus absolute-deadline strategies, timeouts, backoffs, uniqueness identities and the `900s timeout < 1200s retry_after` safety boundary; the source-page job remains intentionally claim-token-owned rather than additionally unique. The finalization watchdog now has a low-cardinality `failed()` log containing only its stable job code and exception class, never exception messages, queue payloads, URLs or tokens. No historical failed job was retried, forgotten or cleared.
- Hardened `app:deployment-check` so an unavailable SQLite backend produces stable secret-free failed checks and exit code 1 instead of aborting during the FTS schema probe. The full SQLite quick/foreign-key integrity gate remains unchanged, while focused regressions now cover a fully ready runtime, pending migrations without execution, 450 failed jobs across bounded chunks, safe aggregation and backend failure without leaking database paths, payloads or exception text.
- Closed account-settings follow-up regressions by preserving a supported session-selected locale for anonymous catalog rendering, keeping the versioned anonymous-storage namespace out of guest HTML while retaining the existing browser key, and aligning authenticated shell navigation with the canonical settings route while profile and security compatibility routes remain available.
- Added one canonical, owner-only account-settings system with localized section routes, stable navigation, private/no-store/noindex responses, typed staged Livewire state, Russian/English labels, accessible mobile forms, IANA timezone selection, explicit browser-timezone suggestion, reduced-motion override, safe errors/loading/reset/confirmation states, and links to the existing profile, security, library, collection, export and deletion workflows. Existing routes remain compatible and no user identifier or private value is accepted in normal settings URLs.
- Added an additive one-row-per-user preference store and a single resolver/service boundary for interface locale, timezone, autoplay, remember-volume, volume/mute, allowed playback speed, real quality/variant preference, subtitles, keyboard shortcuts, reduced motion and the default visibility of newly created collections. Nullable fields preserve explicit choices, `settings_version` protects server/device precedence, rolling-schema reads fall back safely while writes fail closed, and validation prevents arbitrary preference keys, user fields, JSON paths, translated values or media URLs from being stored.
- Integrated preferences into the existing Plyr/HLS player without creating another player: explicit URL selections still win, account quality/variant values are stable eligible codes with safe per-episode fallback, unavailable preferences are retained, device volume/mute is applied and persisted without slider request storms, and playback reset preserves progress, history, bookmarks and library state. A versioned local-storage adapter reads compatible legacy Plyr values, scopes account data with an opaque HMAC and idempotently fills only previously unset account preferences after login.
- Consolidated actual comment and review database-notification controls under `/settings/notifications` while preserving their existing tables, actions and delivery enforcement; collection defaults affect only future collections; exact history/progress/library/private tags remain private; safe browser-session summaries and opaque revocation tokens reuse the established security service; account export now includes non-secret settings and account deletion removes them through the existing lifecycle. Premium/billing, linked OAuth providers, public username/avatar/cover/biography, email/push categories, media-language tracks and additional player/continue-watching modes are absent from the repository and therefore were documented rather than represented by fake controls.
- Hardened account boundaries by allowlisting profile mass assignment, rate-limiting sensitive password/session/deletion actions, formatting profile/history/session/notification dates with the saved locale and IANA timezone, keeping all authenticated responses out of shared caches and social metadata, and preserving current sessions, provider-review/import data, watch state, collections and all legacy routes/keys. No automated tests were created or run per Task 16; verification uses static route/schema/security/translation inspection, formatting, asset compilation, isolated migration rehearsal and browser smoke.

## 2026-07-15

- Added one canonical tagging architecture on top of the existing `tags` and `catalog_title_tag` records without changing current IDs, slugs, source URLs, assignments, catalog routes, playback, library, collections, comments, reviews, or the public Seasonvar import command. Global tags now have opaque stable identity, optional immutable codes, system/editorial/imported/internal types, visibility and moderation lifecycle, Unicode-safe duplicate identity, active/fallback Russian and English content translations, exact aliases, bounded directional synonyms, slug history, provider mappings/provenance, transactional merge/archive workflows, authorized administration, canonical/legacy URL resolution, visible-title counts, dedicated public search/suggestions, related/popular tags, title badges, API resources, catalog filters, recommendations, cache invalidation, SEO and streamed sitemap eligibility.
- Added owner-scoped private personal tags with preserved original Unicode labels, optional original-content locale, exact per-owner duplicate prevention, create/edit/soft-delete/30-day restore, private search and library filtering, staged multi-tag Apply/Cancel on serial pages, idempotent removal, account export/deletion and title-merge reconciliation. Personal tags never become global metadata, public pages, translations, recommendations, search-index documents, sitemap entries or shared-cache data; public user tags/reporting, hierarchy, automatic hashtags and season/episode assignments remain intentionally unsupported rather than becoming competing systems.
- Hardened the final tag rollout after schema, query, privacy and browser inspection: migration-local normalization now exactly matches the runtime Unicode/entity/dash rules before duplicate reconciliation, public eligibility uses an equality-first SQLite index, rolling-schema capability state is request/job scoped, provider/alias/slug races preserve existing canonical ownership, reverse bidirectional synonyms stay unique, imported lifecycle transitions remain importer-owned, newly created personal tags return their persisted optimistic-lock version, and title merge compacts each owner's personal-tag order without losing assignments. Route-backed tag/year state is no longer duplicated in Livewire query strings, compatibility and case variants redirect to one clean canonical URL, nested tag-interface scrolling was removed, and desktop/mobile manual smoke found no overflow or local browser errors.
- Added one canonical title-review architecture by extending the existing imported-review table without changing provider IDs, bodies, source hashes, API route/shape, comments, title identity, playback, library, ratings, search, or sitemap behavior. Verified users can now publish one structured plain-text review per serial with a title, optional existing 1–10 portal rating, server-derived watched snapshot, whole-review spoiler protection, optimistic editing, soft deletion and 30-day restoration; stable aliases preserve direct links through title merges and archived duplicate reconciliation.
- Added idempotent helpful/not-helpful voting, deterministic body-free notifications/preferences, private self history and export, account-deletion anonymization, reports, moderation, temporary/permanent review-only restrictions, anti-spam/rate limits, deterministic sort/filter/pagination, derived public counts/averages, targeted cache invalidation, Russian/English accessible Livewire interfaces and `/admin/reviews`. Season/episode reviews, sentiment, emoji reactions, Markdown/HTML, public author directories, review search/index pages and review JSON-LD remain intentionally unsupported instead of becoming competing or privacy-unsafe systems.
- Hardened the review rollout after final security and privacy inspection: global and per-resource abuse budgets now compose, raw mixed/oversized text is rejected before normalization, owner deletion remains available for an inaccessible title, public queries omit moderation evidence, unresolved `open|reviewed` reports stay actionable, moderation decisions and exact changed-field fingerprints are atomic, and review query URLs keep a clean canonical with `noindex,follow`. The lazy review island never places viewer-specific state or review rows in shared title HTML; an idempotent additive repair also converges environments that applied an early in-flight review schema without rewriting provider data.
- Completed isolated review runtime acceptance across create/edit/moderate/spoiler/delete/restore, helpful vote changes, reports, restrictions, profiles, notifications, direct focus, deterministic pagination, blocks/mutes and account export/deletion. The final hardening replaces a PHP 8.5-incompatible collection callback, carries direct-review identity explicitly into the review island and eagerly loads only direct-link requests, and gives title/profile/admin paginators canonical route paths with validated query state so fallback links and browser history remain correct.
- Added the portal's first canonical comments and discussions domain without replacing imported reviews or changing catalog, playback, library, collection, account, API, SEO, or importer identities. Stable comment IDs now support allowlisted title, season, episode, and collection scopes; bounded flattened replies; escaped Unicode plain text; server-protected spoilers and long bodies; optimistic editing; soft deletion, tombstones, and restoration; idempotent up/down reactions; body-free database notifications and preferences; private blocks and mutes; reports, moderation, temporary/permanent comment restrictions, audit records, anti-spam, rate limiting, deterministic pagination, progressive replies, localized direct links, private activity/export/account-deletion handling, title-merge reconciliation, targeted cache invalidation, and accessible responsive Livewire interfaces in Russian and English. Comments remain outside sitemaps, structured data, search documents, and the mobile API; mentions, premium assets, edit history, public profile activity, and persistent drafts remain intentionally unsupported rather than represented by duplicate models or inactive controls.
- Hardened the final discussion rollout after schema, concurrency, privacy, query and browser inspection: every mutation now re-locks the allowlisted target/comment boundary, retries use desired-state or exact-payload idempotency, material edits re-enter pre-moderation, privacy retirement is terminal, report previews/resolution and moderator thread context are bounded, audit evidence commits atomically, and notification delivery cannot race account deletion or expose body/spoiler text. Private block/mute lists now paginate independently through focused covering indexes; owner tombstone restoration stays reachable; hidden direct links fail closed; moderation and temporary restrictions were verified end to end and reversed in isolated Chromium with zero final console errors.
- Added the canonical serial collections and curated-list system without changing watchlist, ratings, playback progress, history, bookmarks, title identity, or existing catalog URLs. User/editorial/system types, private/unlisted/public visibility, policy-backed ownership, moderation/reporting, soft deletion and scheduled pruning, UUID identity, deterministic slug history, private covers, transactional multi-collection membership, duplicate prevention, manual/automatic ordering, title-merge reconciliation, public profiles, search/filter/pagination, account export/deletion, public API resources, locale-aware editorial translations, safe sharing, targeted cache invalidation, fail-closed rolling-schema readiness, canonical SEO/structured data, and streamed sitemap integration now share one domain boundary; `/lists` and `/selections` remain compatibility aliases rather than a competing architecture.
- Added versioned full-response caching for deterministic guest catalog pages with per-request CSRF/playback URL restoration, bounded recent-URL warming, exact-origin self HTTP requests and post-commit invalidation. Shared HTML is stored as bounded gzip payloads, including the Livewire statistics page while only framework guest session cookies are present. Cache warming coalesces generations/title IDs under `ShouldBeUniqueUntilProcessing` on `cache-warm-v2`; historical `cache-warm` payloads with expired absolute deadlines remain quarantined without destructive retry or cleanup.
- Completed the production fallback path for public-page caching: the verified per-minute Laravel scheduler dispatches a coalesced refresh every ten minutes, optional navigation routes no longer break cached pages during staggered deployments, and catalog/tag reads memoize canonical-schema readiness per HTTP request across rolling migrations. Cold canonical tag pages now render relation constraints and co-occurring tag IDs without type errors before entering the shared response cache.
- Reopened the production-readiness program with a repository/runtime-wide evidence baseline and living phased plan. New dependency, database, security, performance, frontend, Livewire and playback reports record verified state, exact gates, rollback and remaining risks; the baseline exposed production debug mode, two pending migrations, overlapping import cycles, a 24.45-second SQLite-heavy deployment preflight, misleading queue health, a missing cache-warm worker, passive-Blade violations and full-scope static-analysis debt without suppressing them.
- Made production diagnostics truthful and measurable: deployment checks expose safe per-check millisecond durations without payloads or paths, and queue health now reports fixed low-cardinality metrics plus independently scoped idle/processing heartbeats for default, cache-warm, Seasonvar import and title-refresh pools. Heartbeats are read directly so a long-lived process cannot memoize liveness past TTL; operational CLI health fails on degraded while HTTP traffic readiness stays distinct, and live checks surface the real 32k+ backlog instead of a false default-queue green.
- Made queued Seasonvar lifecycle single-flight across admin, CLI and cron, while keeping targeted title refresh independent. Replaced finalizer self-release polling with completion-driven `ShouldBeUniqueUntilProcessing` signals, added a bounded unique watchdog from both Laravel schedule and duplicate cron invocations, and documented the missing production `schedule:run` entry. Tests cover duplicate starts, terminal/non-terminal fan-in, watchdog selection and schedule locks without clearing historical jobs or claims.
- Closed the passive-Blade request/config/auth boundary: all 52 templates now receive prepared state, header/footer/directory links share an immutable navigation view model with precomputed URLs, permissions, active state and complete Tailwind classes, and Livewire directory maxlength is prepared in the component. A zero-tolerance regression rejects infrastructure/container/application static calls and authorization directives; view compilation, Larastan, full PHPUnit, production assets and the 21-scenario desktop/mobile/tablet browser matrix pass.
- Removed an unreachable generated SEO/query matrix that was built and discarded on every request, shrinking `AppLayoutData` from 1,928 to 411 lines and the root layout from 783 to 96 lines. The layout now receives an explicit bounded contract and pre-encoded hex-safe JSON-LD; a 100-iteration rich-payload microbenchmark improved from 23.894 ms median / 25.323 ms p95 to 0.536 / 0.834 ms, while canonical, Open Graph, structured data and breadcrumbs remain covered.
- Added the complete Livewire session-authenticated user portal: Russian registration/login/verification/password recovery, profile and security self-service, device/session/account controls, and a unified filterable `/library/*` for watchlist, ratings, Continue Watching and history. Web and mobile now share account/library services; verified-email policies guard every personal mutation, catalog cards receive bounded query-free personalization, and `/api/v1/me/library/summary` plus documented library filters complete the private API surface.

## 2026-07-14

- Added deterministic offline-first mobile synchronization under `/api/v1`: manifest-first catalog bootstrap, signed owner/scope cursors, 30-day public/private invalidation journal, owner pull, and idempotent 1–50 mutation batches for watchlist, ratings, progress and history. Optimistic per-state versions prevent stale device overwrites; 90-day safe receipts, bounded daily pruning, schema-readiness guards, complete OpenAPI and privacy/regression coverage avoid raw playback/source/token disclosure and keep the existing API available before migration.
- Restricted web and mobile catalog `q` to primary, original and alternate title names only. Descriptions, slugs, provider IDs, people and taxonomy relations no longer enter legacy or FTS results; search document version 3 clears compatibility fields, visible copy states the title-only contract, and production rebuild/tests cover noisy queries such as `Художник 2`.
- Completed mobile playback API v1 without exposing stored media sources: guest/public and ability-aware session creation returns a short-lived same-origin signed encrypted grant, delivery rechecks user/media/hierarchy entitlement before redirect, and verified monotonic progress reuses the canonical web state service. Private/no-store headers, tamper/expiry/revocation tests and OpenAPI schemas lock the contract.
- Exposed mobile owner state across API v1 without duplicating the web domain model: idempotent watchlist/rating desired-state writes, paginated libraries, Continue Watching and viewing history now reuse existing services, policies and visibility rules. Verified-email mutations, cross-user 404 isolation, private no-store responses, fixed-query eager loading and full OpenAPI regressions protect the contract.
- Completed mobile authentication and account self-service for API v1: normalized registration/login, queued email verification and non-enumerating password recovery, 90-day Sanctum device rotation/revocation, `/me` profile/password/delete flows, ability-scoped private responses, cross-user isolation and complete OpenAPI/privacy/rate-limit regressions.
- Completed the public mobile catalog API v1: home, full filter parity with indexed/unindexed arrays, schema-driven Cyrillic/Latin alphabets, 11 directories, title details, audience-safe seasons/episodes/media profiles, bounded suggestions, ranked recommendations and paginated imported reviews. OpenAPI now covers every public v1 GET; privacy, N+1 query-delta and guest-ETag versus Bearer-no-store regressions guard the contract.
- Added the mobile API v1 foundation without breaking legacy JSON routes: `/api` discovery, project-owned OpenAPI 3.1, safe config/health endpoints, stable request-ID error envelopes, 90-day hashed Sanctum tokens with scheduled pruning, and fail-closed private caching for every Authorization request.
- Made Livewire pagination portal-wide and resilient: catalog, directory, viewing-history and admin controls now retain real GET links, preserve route/query filters and scroll to their own refreshed results after morph. Split existing alphabet filters into labeled Cyrillic, Latin `A`–`Z` and symbol groups with deterministic desktop/mobile regression coverage.

## 2026-07-13

- Closed the final bounded repository/runtime backlog: production now runs debug-off with daily warning logs and corrected `www:www` runtime ownership; read-only deployment/failed-job diagnostics, report-only CSP, deterministic Playwright+axe CI, Livewire payload/query budgets, append-only admin audit, Markdown/migration integrity checks and bounded Larastan are implemented. The additive audit migration was rolled out with a verified SQLite backup, all 12 queue workers resumed, health/preflight and public HTTP/CSP smoke passed, and the completed temporary backlog/spec were removed in favor of topic-owner documentation.
- Unified every `/titles` facet inside the full-width «Точный подбор» form: years, publication type, subtitles, actors and all other catalog relations now load through the existing deferred Livewire island between the catalog controls and results. The desktop sidebar, mobile dialog and dialog JavaScript were removed; active filters open the native details automatically, share one total count/reset, preserve the existing query contract and use normal page scrolling.
- Fixed removal of a route-backed taxonomy filter: unchecking the scoped country/genre, removing its active chip or resetting its group now navigates to `/titles`, removes only that taxonomy and preserves independent query filters such as publication type.
- Replaced recommendation `v2` with a local content-aware `v3`: bounded themes and inverse-frequency relation weights enforce a strong relevance gate, generic parser/quality signals cannot manufacture similarity, published playable media remains mandatory, and bounded MMR preserves the strongest first result while diversifying later ranks. Title pages now render one deduplicated ordered list with reason badges and wide `16/10` posters that keep image width and crop only height; global catalog grids are unchanged. Per-title writes revalidate public source/candidate IDs under the write transaction so a concurrent targeted merge cannot abort the catalog-wide snapshot with a foreign-key error.
- Completed the Laravel 13/Livewire 4/video-portal audit with dated environment, security, performance, research, MCP and verification records; configured project-scoped Boost, Context7 and isolated Playwright, fixed Redis-only session connection leakage and production Livewire asset assertions, and introduced a validated HTML-only report-only CSP without exposing provider URLs or changing media delivery.
- Added explicit Seasonvar rights-holder region-block detection: serial fetch/parser now persist normalized provider availability status and check timestamps, full import locally backfills bounded latest snapshots without HTTP, and due blocked pages return through the normal retry planner without proxy/Tor region circumvention.
- Removed duplicate alternate title names and canonicalized all ten catalog relation directories. Alias uniqueness is now independent of alias type; actor/director Cyrillic and Latin equivalents share one record, polluted serial links are rejected, and existing pivots are merged by bounded maintenance. Added a hash-only per-source relation identity registry, so all ten relation types survive provider label/language changes during import or refresh, future adapters can supply stable external IDs, taxonomy pages share the same boundary, and two-phase maintenance preserves mappings through slug collisions without storing raw provider keys.
- Connected the official Playwright MCP as an optional project-scoped Codex server with isolated headless managed Chromium, prompt-approved browser actions, documented browser installation and a real local navigation smoke check.
- Removed completed temporary implementation/QA plans from the current documentation, closed the final bounded backlog, and moved durable contracts into their topic-owner documents.
- Added a reboot-safe single-process `seasonvar:import --forever` systemd profile, made stale refresh jobs for deleted titles successful no-ops, and aligned preparation/finalizer retry deadlines with the longer configured page-claim lease.
- Removed all application-generated HTTP 429 responses: web/API/Livewire transport throttles, action buckets, playback concurrency status, source-health budget and the dedicated Redis limiter workload. Authentication, authorization, CSRF, signed playback URLs, validation and bounded handling of remote Seasonvar/CDN 429 responses remain unchanged.
- Closed the `/titles` crawl trap with `noindex,nofollow`, nofollow UI-state links, ClaudeBot/query robots rules and a Redis-backed query limiter while leaving canonical catalog and card links crawlable.
- Reversed ranked SQLite search to start from materialized FTS candidates and paginated IDs before hydrating only visible cards. Contextual facets now load automatically in a deferred Livewire island, reuse one integer-only `json_each` match set and update linked results live without a «Показать фильтры» step. No search package or database replacement was added.
- Reduced the production SQLite host from 43 queue workers to a persistent 4 import + 8 title-refresh baseline, repaired Blade/cache ownership, stopped the runaway actor aggregate and completed an online backup, two successful integrity checks, WAL checkpoint and `PRAGMA optimize`.
- Added eleven registry-driven public directory hubs for genres, countries, actors, directors, age ratings, translations, statuses, networks, studios, tags and years. One Livewire 4 browser now provides local URL-restorable search/alphabet/decade/sort/pagination state, duplicate-safe published-title counts, responsive accessible cards, Russian/English copy, global Livewire assets, navigation/search suggestions, CollectionPage/ItemList metadata and sitemap coverage; generic `/titles/{type}/{taxonomy}` and year listings remain canonical while validated friendly details redirect permanently.
- Split ready SQLite FTS into a materialized exact/BM25 result query and filter-only rowid queries for paginator totals and facets, preventing SQLite from flattening `MATCH` into a full catalog loop or repeating ranked count work. Search submission now waits for Enter or «Найти», and subtitle availability uses a correlated media `EXISTS`; on the 34,015-document database the former 17.15-second count fell to 7–17 ms and warm full-page samples measured 336–494 ms.
- Rebuilt Seasonvar title refresh as an unbounded application-level fan-out/fan-in pipeline: every known or dynamically discovered season page is prepared independently, one deterministic finalizer applies the complete title manifest without deleting local-only data, global queued import reuses the same groups, and dedicated `seasonvar-title-refresh` workers isolate visitor refresh from catalog backlog.
- Reused the authorized, ordered active-season episode collection for previous/next playback navigation, eliminating both repeated watchable scans inside one season while retaining one fully authorized keyset query at a real season boundary. Five-run production-scale SQLite medians reduced playback SQL by 73.3% and server render by 26.2%; 20-request HTTP p95 fell from 1638.8 to 1037.9 ms with an unchanged response payload.
- Added `seasonvar:import --inventory-only` as a non-catalog sitemap parity mode with typed URL classification, recursive XML/gzip discovery, robots.txt/crawl-delay enforcement, blocked/malformed/unknown accounting, idempotent `SourcePage` storage, import-run/event snapshots, isolated test storage, a single capability registry, and managed `docs/SOURCE_PARITY.md` reporting.
- Fixed infrastructure readiness reporting so a missing queue-worker heartbeat produces an explicit degraded state instead of a false all-clear while database and transport readiness remain available.
- Added browser-initiated background refresh on series pages: SSR stays side-effect free, then one unique queued job updates the canonical page and all known seasons no more often than every 15 minutes, while the complete Livewire title view reflects new metadata, seasons, episodes and media every three seconds without resetting a valid playback selection.
- Fixed live Seasonvar refresh starvation and split season families: distinct titles now use a prioritized multi-worker queue while the 15-minute lock remains per title, targeted refresh merges verified duplicates into the opened card, and the global finalizer consolidates season pages with different provider IDs only when their exact season URL family is proven.
- Replaced the oversized flat advanced catalog form with the compact «Точный подбор» groups for period, series length, rating and video, including explanatory copy, proportional controls, an exact active-count/reset boundary, a preserved GET fallback, and mobile access to view and page-size controls.
- Replaced the three legacy poster/card templates with a class-based shared system: `x-ui.poster-frame` is the only catalog artwork image boundary, `x-ui.poster-card` provides strict grid/horizontal/compact one-outline shells, and query-free `x-catalog.title-card` serves catalog, search and recommendations. Homepage media, title hero, viewing activity and guarded stats posters now share cover plus 2% overscan behavior without changing the player poster attribute; title-level playback also marks its guaranteed-null episode relation as loaded to prevent strict lazy-loading failures.
- Removed the redundant `/titles` summary cards, added reduced-motion-aware smooth pagination return to the result boundary, widened the title player to the full panel and moved the responsive “Ваш сериал” controls below it. Public display accessors now separate a matching `/original_title` suffix while preserving the source/search data and unrelated slash names.
- Added a production cache architecture with separate Redis cache/session/queue/limiter/lock connections, disposable Memcached hot storage, canonical versioned keys, bounded TTL jitter/stale windows, Redis single-flight locks, after-commit invalidation, deduplicated warming, public HTTP validators, readiness and low-cardinality cache metrics. Homepage/default public facets/stats now use compact DTO/ID snapshots rather than cached Eloquent graphs; private state, signed media URLs and raw access tokens remain uncached.
- Added real Redis/Memcached CI integration and outage tests, cache-warm systemd worker, centralized cache/environment runbooks, and removed cross-request caching of the Google service-account raw access token.
- Fixed a production-compatible session regression: the dedicated Redis `sessions` connection is selected only for the Redis session driver, so an existing database-session environment no longer fails public requests with an unknown SQL connection.
- Changed new-environment logging defaults from one unbounded file to the existing daily rotating channel; production still has to set the documented non-debug environment values outside Git.
- Canonicalized unindexed multi-value catalog query strings before Livewire hydration, preserving valid pagination while removing malformed dotted duplicate keys and producing stable indexed arrays for refresh and browser history.
- Replaced the homepage watchable-title aggregate ordering with a visibility-first existence query, introduced precisely invalidated public headline metrics, and added an additive `(status, published_at, id)` media-feed index migration justified by the inspected SQLite plan.
- Kept cache-version invalidation on the critical Redis locks workload, so a disposable domain-cache outage cannot preserve a superseded Memcached namespace after a catalog mutation; a real Redis/Memcached regression covers this failure path.
- Added a global actor-level Livewire request budget above per-feature limits, preventing malformed clients from multiplying their allowance by cycling through known component methods while retaining stricter domain action throttles.
- Prevented recommendation rebuild exhaustion at 128 MiB by preselecting shared candidate features in grouped SQL and capping Eloquent profile hydration at the measured safe chunk of 100 instead of building and then compacting tens of thousands of singleton PHP hash buckets; exact scoring and visibility rules are unchanged.
- Replaced the stale one-time audit with an unlimited living P0–P4 improvement plan covering security, playback, importer reliability, database integrity, filtering, search, performance, caching, Livewire, accessibility, responsive behavior, localization, observability, testing, CI, deployment, documentation, maintenance and product development.
- Added a single documentation ownership map for Livewire, catalog queries and URL state, domain visibility/navigation/user progress, playback authorization, importer/queues, cache invalidation, security, verification and deployment; README and code standards now link to topic owners instead of repeating the full workflow.
- Strengthened the versioned Git guards with explicit unresolved-conflict, temporary/debug-file and environment/credential-path checks on commit and push, plus a dependency-free `composer hooks:install` command wired into project setup; all failures are read-only and explain how to recover.
- Added Russian/English catalog translation domains and framework pluralization for result, season, episode, rating, importer, and viewing-history counts; newly touched Livewire/Blade UI no longer embeds interface copy in component code.
- Hardened title SSR metadata with plain-text sanitization, locale-aware HTML/Open Graph/WebPage language, verified-content fallbacks, initial-response title/description rendering, and no inferred `TVSeries.inLanguage` from UI or media labels.
- Canonicalized complex/search catalog URLs to the unfiltered catalog while retaining indexable taxonomy/year routes, and added additive historical-slug redirects that survive admin edits and importer duplicate merges without bypassing publication access.
- Flattened title-page quick navigation and kept plain catalog navigation free of decorative borders/rings, using typography, restrained backgrounds, icons, and a narrow active marker while preserving borders on forms, alerts, media frames, and structural cards.
- Standardized a visible two-layer keyboard focus treatment across links, buttons, inputs, selects, textareas, and search frames; enlarged sort, view, page-size, alphabet, filter, and header targets to at least 44 px and improved semantic season navigation/progress indicators.
- Hardened responsive rendering for long taxonomy labels, counts, Continue Watching cards, importer metadata/actions/states, ratings, episode media counts, and player retry/watchlist loading controls without recreating the `wire:ignore` player during viewport changes.
- Added the authorized `/admin/catalog` Livewire 4 workspace for bounded title lookup, editorial metadata, publication windows, actor/director/genre/country/translation relations, regular/special seasons and episodes, and allowlisted external video sources; existing importer runs remain in the linked `/admin/imports` workflow.
- Added policy-backed explicit field allowlists, hierarchy-scoped model resolution, per-action throttling, reversible hide/unpublish actions, short row-locking transactions, and version fingerprints that reject stale concurrent edits while preserving progress, history, ratings and watchlists.
- Reused existing database unique keys and importer ownership rules instead of adding schema: pivot attachment is idempotent, provider baselines remain intact, publication changes refresh SQL visibility/index timestamps and precisely invalidate stats/recommendations without exposing stored source URLs.
- Hardened sensitive Livewire actions with independent user/resource rate-limit buckets for catalog search, playback-session issuance, progress, ratings, watchlist, history and import administration; source-health probes now stop locally without recording a provider failure when their host budget is exhausted.
- Closed SSRF and credential-leak boundaries by pinning verified public DNS for stats posters and external playlists, refusing unsafe redirects/credentialed URLs, restricting Google service-account JWT exchange to the canonical token endpoint, and redacting targeted importer URLs from logs and queued mail notifications.
- Strengthened media-profile allowlists and private-upload path normalization, including absolute, drive, backslash, dot-segment and null-byte rejection, and added malformed/direct-access regression coverage in existing security, importer and storage tests.
- Reduced catalog page-builder queries from 20 to 11 by batching ten own-group-excluded relation facets into one bounded UNION, while keeping per-group limits, duplicate-free totals, fresh lifecycle semantics, and server-side actor/director search.
- Constrained catalog/title/home taxonomy eager loads to rendered columns, reduced episode playback resolution from six queries to two by reusing the authorized hierarchy, and kept direct signed playback independently authorized.
- Reduced importer dashboard polling to five bounded queries, combined health/due counters into one covering UNION round trip, and changed media-health backlog predicates so SQLite uses the existing health index instead of a full table scan; query plans did not justify new indexes.
- Added centralized licensed-media health states (`active`, `degraded`, `unavailable`, `disabled`) with atomic failure counters, last-success/error/latency metadata, bounded exponential retries, configurable failure thresholds, and automatic recovery into playback eligibility.
- Hardened source probes with HTTPS/provider allowlists, public-DNS validation and per-request DNS pinning, blocked credentials/private/link-local/metadata targets and redirects, strict connect/total timeouts, bounded streamed Range/manifest reads, safe error categories, and fully redacted operational events.
- Integrated health state into playback fallback, public episode/media counts, refresh planning, queued-import finalization, stats cache refresh, and an aggregate-only `/admin/imports` health panel; added an additive migration with safe legacy-state backfill and a due-check index.
- Added the authorized `/admin/imports` Livewire operations screen with duplicate-safe queued starts, active-only visible polling, cooperative cancellation, retry audit links, stale-running recovery, bounded counters, and sanitized errors.
- Added a scalar-payload unique coordinator job with explicit attempts/backoff/timeout, transient/permanent failure classification, queued/running/completed/partial/failed/cancelled status transitions, and heartbeat tracking across coordinator, page, and finalizer jobs.
- Added nullable requester/retry foreign keys and heartbeat/cancellation indexes to import runs, expanded queue diagnostics to include queued coordinators, and centralized credential/URL/path redaction for persisted and logged importer failures.
- Added a validated normalized Seasonvar DTO boundary, provider-ID/canonical-URL title identity resolution, stable person-URL disambiguation, and additive lookup indexes without unsafe name-only merging.
- Preserved editorial title/description/artwork through a three-way provider baseline, kept publication/audience/windows/soft deletes under local ownership, and prevented partial snapshots or repeat imports from restoring/deleting relationships and media.
- Centralized catalog and playback access in `CatalogEntitlementService`, with one SQL visibility boundary and one structured loaded-release decision reused by search, route binding, recommendations, policies, progress/history, source resolution, and direct signed playback rechecks.
- Added explicit authentication, plan, region, profile, and concurrency decision states without inventing profile, PIN, role, billing, territory, or stream-session storage; the authenticated `User` remains the only supported active profile.
- Consolidated favorites into the existing watchlist concept and replaced toggle read-modify-write with authorized desired-state conditional writes protected by the existing user/title unique key; repeated Livewire requests do not change timestamps, no-op removals create no rows, and browser user/profile IDs are never accepted.
- Centralized the configurable internal rating range, added one-query user watchlist/rating aggregates with immediate create/change/remove updates, and kept imported provider ratings fully separate from viewer averages.
- Added the authenticated `/watching` Livewire page with one continue-watching action per accessible series, real-playback-only paginated history, Russian loading/empty/unavailable states, profile-scoped removal, and typed full-clear confirmation.
- Derived continue/history state from canonical episode progress using fixed-query window ranking, deterministic release-lane navigation, batch hydration, and a new user/history ordering index; newly published episodes re-qualify completed series without cache invalidation.
- Hardened persistent episode progress around the existing unique user/episode record: trusted media duration, percentage, first-start time, source media, opaque expiring playback sessions, monotonic event sequences, transactional retry-safe updates, and non-regressing completion state now feed the same history and continue-watching query.
- Rebuilt the Plyr/HLS browser lifecycle around one guarded session per signed source: async initialization is generation-safe, listeners/timers/resources share AbortController cleanup, progress uses a playing-only heartbeat plus bounded lifecycle flushes, stale Livewire session events are rejected, and fixed Russian loading/retry/error states reveal no provider details. Cleanup also clears markers from the original media node restored by Plyr so Livewire navigation and the browser back-forward cache can initialize it exactly once again.
- Centralized playback authorization and source selection in `CatalogPlaybackSourceResolver`: Blade/Livewire now receive a short-lived signed, viewer-bound internal URL instead of the stored provider URL, direct access rechecks the complete publication hierarchy and source ownership, and video sitemaps publish only internal player locations.
- Added provider/format/quality preference ranking, known-failure exclusion from public episode media/counts, explicit Russian playback availability states, an HTTPS/DNS provider allowlist, redirect-free streamed availability probes, bounded response metadata, redacted probe events, and playback endpoint throttling.
- Added publication-aware previous/next episode navigation to the Livewire title player. Navigation stays inside the current regular/special release lane, crosses into the next or previous visible season, skips inaccessible or source-less episodes, and keeps season state shareable in the URL.
- Added defensive labels and null-safe keyset ordering for missing release numbers while treating `sort_order` as the provider sequence. Livewire season changes retain watchlist/rating state, browser-supplied season/episode IDs are revalidated against the visible title hierarchy, and each multi-field playback change now creates one coherent browser-history entry.

## 2026-07-12

- Reworked catalog totals and facets around unique visible title IDs: relation, year, publication-type, and subtitle counts now use own-group-excluded context, bounded grouped aggregates, selected zero-count retention, and no stale cross-request cache.
- Applied actor/director top-N limits after contextual filtering, removed misleading global count denominators from `/titles`, and added lifecycle/query-budget regression coverage without introducing per-option queries.
- Hardened catalog search, sorting, pagination, and Livewire URL state as one validated flow: NFKC/whitespace normalization, safe fallbacks for malformed scalar values and unsupported sort keys, 650 ms search debounce, stable browser-history state, and automatic recovery from out-of-range pages.
- Added indexed exact external-provider-ID search and the additive `catalog_title_aliases(name_hash, catalog_title_id)` lookup index while preserving visibility-first SQL subqueries, duplicate-free totals, and deterministic allowlisted sorting.
- Added consistent OR-within-group and AND-between-group catalog matching for years, all existing taxonomy relations, publication types, video qualities, and subtitle availability, with normalized bounded URL arrays and per-value/group resets.
- Added bounded debounced server-side actor/director option search, canonical publication-type aliases, duplicate-free grouped pivot subqueries, and regression coverage for invalid/missing URL values, exact paginator totals, Livewire state preservation, and multi-group combinations.
- Hardened Livewire browser-history hydration for empty URL fields and corrected the visible active-filter count to include fixed list and advanced scalar groups.
- Isolated the PHPUnit rate-limiter store in memory so prior browser QA cannot make otherwise independent `/stats` feature tests fail with HTTP 429.
- Refactored the main `/titles` catalog into a full-page Livewire 4.3 component with bounded URL-synchronized state, debounced server search, multi-select and advanced filters, deterministic sorting, stable pagination, group/full reset actions, loading/error/empty feedback, and no Eloquent collections in the public snapshot.
- Preserved the existing GET form fallback and centralized `CatalogTitlesRequest` validation plus the shared `CatalogTitlesPageBuilder`/`CatalogTitleQuery` data path; added Russian Livewire pagination and regression coverage in existing feature tests.
- Centralized public title visibility, normalized catalog filtering, facet counts, API queries, sitemap/feed selection, public statistics, and recommendation candidates in the reusable `CatalogTitleQuery` layer.
- Kept pivot and search candidates in grouped SQL subqueries to prevent duplicate titles, accurate paginator totals, and full ID collection materialization; all mapped sorts now share a deterministic title ID tie-breaker.

## 2026-07-09

- Added an optional queued email notification for failed queued Seasonvar imports, with safe env configuration, dispatch/content tests, and notification documentation.
- Added a private upload storage foundation with explicit image validation rules, generated filenames, fake-storage tests, and storage documentation.
- Improved catalog search form UX with reusable Blade form components, visible validation errors, and preserved old input after validation redirects.
- Cleaned up Eloquent relationship inverses for catalog source pages and Seasonvar import runs, added schema-aligned casts, and documented model query rules in `docs/models.md`.
- Synchronized Markdown documentation with the current Laravel 13 routes, setup, MCP, deployment, testing, API, Blade, and CI conventions.
- Added a GitHub Actions CI workflow for Composer, Pint, Laravel tests, PHP syntax linting, npm audit/build, and dependency audits.
- Documented deployment environment requirements, expanded non-secret `.env.example` defaults, and added a regression test that keeps `env()` calls inside config files.
- Improved the Vite frontend build by using one app entry, lazy-loading Plyr/HLS player assets, loading generated Vite fonts, and documenting frontend commands.
- Added a read-only catalog titles JSON API with explicit Laravel API Resources, eager-loaded relationships, pagination metadata, and sensitive-field regression tests.
- Added a lock-aware queued Seasonvar import job with explicit timeout, retries, backoff, uniqueness, failure logging, and aligned local queue worker settings.
- Added conservative web security headers for Laravel responses.
- Added a named rate limiter for the public read-only `/stats` diagnostics route.
- Disabled Laravel local temporary storage routes by default via `LOCAL_FILESYSTEM_SERVE=false`.
- Documented security rules for secrets, external URLs, Blade output, and dependency audits.
- Added regression tests for security headers, stats throttling, disabled storage routes, and private-host playlist URL blocking.
