@props(['canVerify'])
<x-ui.panel :title="__('issues.detail.verify_title')" icon="fa-solid fa-rotate">
    <label for="reopen-reason" class="block text-sm font-bold">{{ __('issues.fields.reopen_reason') }}</label><textarea id="reopen-reason" wire:model="reopenReason" rows="4" maxlength="2000" class="mt-2 w-full rounded-control border border-slate-300 p-3"></textarea><div class="mt-3 flex flex-wrap gap-2">@if ($canVerify)<button type="button" wire:click="verify(true)" wire:loading.attr="disabled" class="min-h-11 rounded-control bg-emerald-700 px-3 font-bold text-white">{{ __('issues.actions.fixed') }}</button><button type="button" wire:click="verify(false)" wire:loading.attr="disabled" class="min-h-11 rounded-control bg-amber-700 px-3 font-bold text-white">{{ __('issues.actions.still_broken') }}</button>@else<button type="button" wire:click="reopen" wire:loading.attr="disabled" class="min-h-11 rounded-control bg-amber-700 px-3 font-bold text-white">{{ __('issues.actions.reopen') }}</button>@endif</div>
</x-ui.panel>
